<!DOCTYPE html>
<html>

<head>
  <%- include('../partials/authhead') %>
</head>
<body>
  <%- include('../partials/authnav') %>
    <div class="container mb-3">

      <h3 class="contentBoxTop mb-3">인증번호 입력</h3>

      <form action="/users" method="post">

        <div class="form-group row">
          <label for="username" class="col-sm-3 col-form-label">5 digit*</label>
          <div class="col-sm-9">
            <input type="text" id="username" name="username" value="<%= user.username %>"
              class="form-control <%= (errors.username)?'is-invalid':'' %>">
            <% if(errors.username){ %>
              <span class="invalid-feedback">
                <%= errors.username.message %>
              </span>
              <% } %>
          </div>
        </div>

        <div class="form-group">
              
          <input type="button" onclick="test()" value="확인" class="btn btn-primary">
          <script>
            function test(){
            alert("test");
            var isAuth = true;
            if(isAuth==true){
            window.opener.location.href="/users/new";
            window.close();
            }
            else {
            window.opener.location.href="/users/auth";
                }
            }
            
            </script>
              
              <input type="button" onclick="test1()" value="취소" class="btn btn-primary">
              <script>
                function test1(){
                window.close();
                }
                </script>
          
        </div>

<!-- 
        <div class="form-group row">
        <label for="smsValidation" class="col-sm-3 col-form-label">인증번호*</label>
        <div class="col-sm-9">
          <input type="text" id="smsValidation" name="smsValidation" value="<%=user.sms %>"
            class="form-control <%= (errors.sms)?'is-invalid':'' %>">
          <% if(errors.sms){ %>
            <span class="invalid-feedback">
              <%= errors.sms.message %>
            </span>
            <% } %>
        </div>
      </div>

      <div class="form-group">
        <a class="btn btn-primary" href="../">뒤로가기</a>
      </div> -->


        <!-- <% if(errors.unhandled){ %>
          <div class="alert alert-danger">
            <%= errors.unhandled %>
          </div>
          <% } %> -->                
      </form>
    </div>
</body>

</html>